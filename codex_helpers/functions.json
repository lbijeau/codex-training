[
  {
    "name": "list_todos",
    "description": "Collect TODO comments from specified files",
    "parameters": {
      "type": "object",
      "properties": {
        "paths": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Paths (globs or files) to scan for TODO comments"
        }
      },
      "required": ["paths"]
    }
  },
  {
    "name": "run_tests",
    "description": "Run the test suite or a subset of tests and report the outcome",
    "parameters": {
      "type": "object",
      "properties": {
        "target": {
          "type": "string",
          "description": "Optional pytest target (file or directory)"
        }
      },
      "required": []
    }
  }
]
