# Helper Documentation Template

**Helper name:**

**Purpose:**

**Schema:**
```
{
  "type": "object",
  "properties": {
    "...": {"type": "string"}
  },
  "required": [...]
}
```

**Usage examples:**
1. Prompt: ...
2. Helper output: ...
3. Codex follow-up: ...

**Validation:**
List validation scripts (e.g., `scripts/validate_ranges.py`) that guard this helper.

**Notes:**
- Security considerations
- Logging metadata
